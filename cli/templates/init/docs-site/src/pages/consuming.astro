---
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import Mermaid from '../components/Mermaid.astro';
import { withBase } from '../lib/specs';

// The plugin and skills live in the sysspec repo, not this spec repo.
// `sysspec init` substitutes the token; in the sysspec repo itself the
// token survives untouched, so fall back to its home.
const slug = '__SYSSPEC_REPO_SLUG__';
const sysspecRepo = slug.startsWith('__') ? 'hungovercoders/sysspec' : slug;

// Same boundary as the implementation loop, one lane wider: a consumer
// never builds the service, it builds against the pinned mocks.
const flow = `flowchart TD
  subgraph specs["Spec repo (the specs of record)"]
    tag["merge to main cuts<br/>&lt;service&gt;/vX.Y.Z"]
  end
  subgraph consumer["Consumer repo (UI, client, downstream system)"]
    lock["contracts.lock<br/>pins the tag + its commit sha"]
    fetch["task contracts:fetch<br/>read-only .contracts/ (specs + mocks + cli)"]
    mocks["task -d .contracts mocks:load<br/>pinned mocks up, fixtures included"]
    build["build<br/>generate types from the fetched specs"]
    verify{"verify against the mocks"}
    done(["consumer satisfies<br/>the pinned surface"])
  end
  tag -->|"pin the newest tag"| lock
  lock --> fetch --> mocks --> build --> verify
  verify -->|green| done
  verify -->|red| build
  tag -.->|"each new tag: Renovate re-pins"| lock`;

---
<StarlightPage frontmatter={{ title: 'Build a consumer' }}>
  <p>
    A consumer (a UI, client app, or downstream system) implements none of
    these contracts; it relies on them. The Microcks mock stack stands in
    for the real services, so a consumer can be built and demonstrated,
    with data, before any implementation exists. This is what the mocks are
    for: consumers, and the spec repo's own conformance checks. Holding a
    real implementation to the contracts is a different job with a
    different command, covered in <a href={withBase('/implementing/')}>Drive an implementation</a>.
  </p>
  <p>
    The loop below is carried end-to-end by the
    <code> consume-service</code> skill. Install the plugin
    (<code> /plugin marketplace add {sysspecRepo}</code>, then
    <code> /plugin install</code>) and ask to build against a service; the
    skill runs everything on this page, from interview to verified
    definition of done. What follows is the shape of that loop, so you can
    recognise it, review it, or run it by hand.
  </p>
  <Mermaid chart={flow} />

  <h2>1. Pin and fetch, exactly like an implementation</h2>
  <p>
    The consumer records which released surface it builds against in a
    <code> contracts.lock</code> and fetches it read-only into
    <code> .contracts/</code>, using the same lock and <code>contracts:fetch</code>
    the implementation journey uses, mock stack included, so the mocks a
    consumer sees are versioned by the pin. A consumer may pin more than
    one service.
  </p>

  <h2>2. Stand up the pinned mocks</h2>
  <pre><code>{`task -d .contracts mocks:load SERVICE=<service>   # stack up + pinned specs loaded
task -d .contracts mocks:watch CHANNEL=<Title>/<version>/<operation>
task -d .contracts mocks:down`}</code></pre>
  <p>
    Point the client at the mocks (title and version come from the spec's
    <code> info</code> block):
  </p>
  <ul>
    <li>
      <strong>REST</strong> mocks answer at
      <code> http://localhost:8585/rest/&lt;Title&gt;/&lt;version&gt;/…</code>
      with fixture data included, so list and detail screens render
      real-looking aggregates.
    </li>
    <li>
      <strong>Events</strong> arrive on
      <code> ws://localhost:8081/api/ws/&lt;Title&gt;/&lt;version&gt;/&lt;operation&gt;</code>,
      which emits the example CloudEvents on a schedule.
    </li>
  </ul>

  <h2>3. Build against them</h2>
  <p>
    Generate client types from the fetched OpenAPI and the AsyncAPI message
    schemas; never hand-model a payload from memory or from observed mock
    traffic. Honour the event conventions the features state: dedupe on the
    CloudEvents envelope <code>id</code>, expect at-least-once delivery,
    assume ordering only within an aggregate, and tolerate additive change,
    because a minor bump must never break a consumer.
  </p>

  <h2>4. Verify (the consumer's definition of done)</h2>
  <ol>
    <li>
      <strong>Client flows against the REST mocks.</strong> Every call the
      consumer makes, with responses parsed through the generated types.
    </li>
    <li>
      <strong>Event handling against real envelopes.</strong> Feed the
      handler from the WS mock or the pinned example files, validating each
      payload against the AsyncAPI schema before acting on it.
    </li>
    <li>
      <strong>Idempotence.</strong> Replaying the same envelope
      <code> id</code> must not double-apply. The features promise
      at-least-once delivery, so this is contract rather than hygiene.
    </li>
    <li>
      <strong>Falsifiability.</strong> Prove the suite can fail by running it
      once against the CLI's null service
      (<code>task -d .contracts null:run …</code>) or, minimally, with the
      mock stack down, and require zero passes. A check that stays green
      against a service answering <code>200 {'{}'}</code> to everything
      verifies nothing.
    </li>
  </ol>
  <p>
    Green proves the consumer satisfies the pinned surface's examples and
    schemas. It does not prove the real service behaves; that is the
    implementation's verification loop, on the other page.
  </p>

  <h2>5. Stay current</h2>
  <p>
    Identical machinery to implementations: new release tags arrive as
    Renovate pin-bump PRs, the suite above runs against the new pin with
    new mocks and new fixtures, and green minors auto-merge with no human
    and no agent. The <code>consume-service</code> skill
    (<a href={`https://github.com/${sysspecRepo}`}>{sysspecRepo}</a>)
    carries the full walkthrough.
  </p>
</StarlightPage>
