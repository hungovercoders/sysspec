name: pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    uses: __SYSSPEC_REPO_SLUG__/.github/workflows/sysspec-pages.yml@__KIT_MAJOR__
