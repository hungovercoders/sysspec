#!/bin/sh
# The fast tier before every commit: lint and the diff gates. The docs
# build runs on push (pre-push) and in CI.
# git exports GIT_DIR/GIT_INDEX_FILE to hooks; they leak into subprocesses
# (the docs build) and point them at this repo's git dir.
unset GIT_DIR GIT_INDEX_FILE GIT_WORK_TREE GIT_COMMON_DIR GIT_PREFIX
exec task check:fast
