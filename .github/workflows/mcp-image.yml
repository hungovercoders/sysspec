name: mcp-image

on:
  push:
    branches: [main]
    paths: ["specs/**", "mcp/**", ".github/workflows/mcp-image.yml", ".github/workflows/sysspec-mcp-image.yml"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: mcp-image
  cancel-in-progress: true

jobs:
  publish:
    uses: ./.github/workflows/sysspec-mcp-image.yml
    with:
      # This repo builds the server from source, not the published package.
      dockerfile: mcp/Dockerfile
      image-name: sysspec-mcp
