{
  "name": "website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node ../scripts/third-party-notices.mjs --title \"the sysspec website\" --packages .astro/bundled-packages.json --add @fontsource-variable/inter --add @fontsource-variable/jetbrains-mono --add pagefind --add astro-expressive-code --add expressive-code --add @expressive-code/core --out-dir dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.10",
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "astro": "^7.2.9"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
